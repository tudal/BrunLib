package pl.brun.lib.util.func {
	import pl.brun.lib.util.DisplayUtils;
	import flash.display.DisplayObject;
	/**
	 * @author Marek
	 */
	public function disposeDisplay(display:DisplayObject):void {
		DisplayUtils.disposeDisplay(display)
	}
}
