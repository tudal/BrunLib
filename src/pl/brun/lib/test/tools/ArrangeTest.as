package pl.brun.lib.test.tools {
	import pl.brun.lib.test.TestBase;

	/**
	 * @author Marek Brun
	 */
	public class ArrangeTest extends TestBase {
		public function ArrangeTest(isDebugger:Boolean = true, isDarkBG:Boolean = false, fps:uint = 40, isBG:Boolean = true) {
			super(isDebugger, isDarkBG, fps, isBG);
		}
	}
}
